<UserControl x:Class="PVPlus2.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PVPlus2.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800"
             d:DesignHeight="450"
             Background="{DynamicResource RegionBrush}">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10" LastChildFill="False">
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Left">
                <Button Width="120"
                        Height="32"
                        Margin="0,0,10,0"
                        Content="Parlot"
                        Command="{Binding RunTestParlotCommand}" />

                <Button Width="220"
                        Height="32"
                        Margin="0,0,20,0"
                        Content="TotalTest"
                        Command="{Binding TotalTestCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Right">
                <TextBlock Margin="0,0,8,0"
                           VerticalAlignment="Center"
                           Text="Array Length" />

                <TextBox Width="120"
                         Height="32"
                         VerticalContentAlignment="Center"
                         Text="{Binding ArrayLength, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </DockPanel>

        <TextBox Grid.Row="1"
                 Margin="0,0,0,10"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" />

        <TextBox Grid.Row="2"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Text="{Binding OutputText}" />

    </Grid>
</UserControl>
